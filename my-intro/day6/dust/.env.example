# data.go.kr(공공데이터포털)에서 발급받은 에어코리아 "대기오염정보 조회 서비스" 서비스키.
# 반드시 Decoding 키를 넣을 것 — Encoding 키(퍼센트 인코딩된 문자열, 예: %2B 포함)를 넣으면
# fetch가 다시 인코딩해서 이중 인코딩이 되고, 매 요청이 원인을 알기 힘든 인증 오류로 실패한다.
AIRKOREA_SERVICE_KEY=
